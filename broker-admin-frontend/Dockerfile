# установка основы нового образа из готового образа с уже предустановленным Node.js версии 12 и соответствующего NPM
FROM node:12
# создание директории приложения и выбор его как рабочего каталога
WORKDIR /usr/src/app
# установка зависимостей
# символ астериск ("*") используется для того чтобы по возможности
# скопировать оба файла: package.json и package-lock.json,
# но использование package-lock.json может вызвать ошибку!
# COPY package*.json ./
COPY package.json ./
# скачивание зависимостей, определенных в файле package.json
RUN npm i
# копируем внутрь образа в корень (вторая точка) весь исходный код, исключая указанные в файле .dockerignore каталоги и файлы
COPY . .
EXPOSE 3001
# запускаем веб-приложение внутри контейнера
# CMD [ "npm", "start" ]